{
  "name": "sudo-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check admin.js && node --check feedback.js && node --check feedback-admin.js",
    "test": "node tests/feedback-system.test.cjs"
  },
  "dependencies": {
    "better-sqlite3": "^12.4.6",
    "express": "^5.1.0",
    "sharp": "0.35.4"
  },
  "devDependencies": {
    "playwright": "^1.60.0"
  }
}
